// Syncthing never syncs .stignore, so this file is per-device and drifts.
// Keep it to the single include below; the real patterns live in
// .stignore.shared, which is not a reserved name and so does sync.
#include .stignore.shared
