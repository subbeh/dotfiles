colors:
  fg: "{{ .Vars.color.fg.default }}"
  bg: "{{ .Vars.color.bg.default }}"

  white:          "{{ .Vars.color.white.base }}"
  grey:           "{{ .Vars.color.bg.lightest }}"
  red:            "{{ .Vars.color.red.base }}"
  red_bright:     "{{ .Vars.color.red.bright }}"
  green:          "{{ .Vars.color.green.base }}"
  green_bright:   "{{ .Vars.color.green.bright }}"
  yellow:         "{{ .Vars.color.yellow.base }}"
  yellow_bright:  "{{ .Vars.color.yellow.bright }}"
  blue:           "{{ .Vars.color.blue.base }}"
  blue_bright:    "{{ .Vars.color.blue.bright }}"
  magenta:        "{{ .Vars.color.magenta.base }}"
  magenta_bright: "{{ .Vars.color.magenta.bright }}"
  cyan:           "{{ .Vars.color.cyan.base }}"
  cyan_bright:    "{{ .Vars.color.cyan.bright }}"
  orange:         "{{ .Vars.color.orange.base }}"

core:
  normal_text: {}
  regular_file: {}
  reset_to_normal: {}

  directory:
    foreground: blue_bright
    font-style: bold

  symlink:
    foreground: magenta

  multi_hard_link: {}

  fifo:
    foreground: fg
    background: blue

  socket:
    foreground: bg
    background: magenta
    font-style: bold

  door:
    foreground: bg
    background: magenta
    font-style: bold

  block_device:
    foreground: cyan
    background: grey

  character_device:
    foreground: magenta
    background: grey

  broken_symlink:
    foreground: fg
    background: red

  missing_symlink_target:
    foreground: fg
    background: red

  setuid: {}

  setgid: {}

  file_with_capability: {}

  sticky_other_writable: {}

  other_writable: {}

  sticky: {}

  executable_file:
    foreground: green_bright
    font-style: bold

text:
  special:
    foreground: bg
    background: yellow

  todo:
    font-style: bold

  licenses:
    foreground: grey

  configuration:
    foreground: yellow

  other:
    foreground: yellow

markup:
  foreground: yellow

programming:
  source:
    foreground: cyan

  tooling:
    foreground: cyan

    continuous-integration:
      foreground: cyan

media:
  foreground: magenta

office:
  foreground: red

archives:
  foreground: cyan
  font-style: underline

executable:
  foreground: red
  font-style: bold

unimportant:
  foreground: grey
